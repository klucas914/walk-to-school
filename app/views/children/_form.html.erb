<%= form_for @child do |f| %>
  
<% if @child.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@child.errors.count, "error") %> prohibited
        this child from being saved:
      </h2>
      <ul>
        <% @child.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
	<%= f.label "Child Name" %><br>
	<%= f.text_field :child_name %>
  </p>

  <p>
  	<%= f.label "Birthdate" %><br>
  	<%= f.date_field :child_birthdate %>
  </p>

  <p>
	<%= f.label "Upload an Image" %><br>
	<!--<%= f.file_field :child_image %>-->
  </p>
  <p>
	<%= f.submit %>
  </p>
<% end %>